import User from "../src/models/modelsUser";
import { utilsResponseHandler} from "../utils/utilsResponseHandler";

// POST
const registerUser = async(req, res) => {
    try {

    } catch (error) {
        cons
    }
}